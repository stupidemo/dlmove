[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dlmove"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "watchdog",
    "pyyaml"
]

[project.scripts]
dlmove = "dlmove.cli:main"